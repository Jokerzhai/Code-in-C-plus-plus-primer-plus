//using clock() in a time delay loop
#include <iostream>
#include <ctime>    //describe clock() function ,clock t type
using namespace std;

int main()
{
    cout << "Enter the delay time , in seconds : " ;
    float secs;
    cin >> secs;
    clock_t_delay = sec * CLOCKS_PER_SEC;       //convert to clock ticks
    cout << "Starting \a\n";
    clock_t_delay = clock();
    while(clock().start < delay)
    return 0;
}
